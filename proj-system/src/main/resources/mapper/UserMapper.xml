<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.org.mollcol.mapper.UserMapper">

    <insert id="insertUser">
        insert into users
            (user_id, user_name, first_name, last_name, email, password, date_of_birth, registration_date)
        values
            (#{userId}, #{userName}, #{firstName}, #{lastName}, #{email}, #{password}, #{dateOfBirth}, #{registrationDate})
    </insert>
</mapper>